### --- .gitattributes --- ###

# ========================================================
# 1. Webデモ (www) は「ドキュメント/事例」扱いとして統計から外す
# ========================================================
# これにより、JSが大幅に減り、RustとPythonが主役に。
www/* linguist-vendored

# ========================================================
# 2. Python (tests) は「重要な構成要素」としてカウントさせる
# ========================================================
# 通常テストコードは除外されるが、このプロジェクトでは
# Pythonバインディングの重要性が高いため、明示的に残す。

# ========================================================
# 3. Rust (src) は当然そのままカウント
# ========================================================
